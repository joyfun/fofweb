
export default {
  state: {
    Personinfo:{
      name: 'test',
      id:'1',
      email:"",
    },
  },
  mutations: {
    closeTab(state,val) {
      // let result = state.tablist.findIndex(item => item.name === val.name)
      // state.tablist.splice(result,1)
    }
  },
  actions: {}
}
